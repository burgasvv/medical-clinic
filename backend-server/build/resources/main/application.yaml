
ktor:
  deployment:
    host: 0.0.0.0
    port: 9000
  application:
    modules:
      - org.burgas.MainKt.modules

postgres:
  url: '$POSTGRES_URL:jdbc:postgresql://localhost:5000/medical-clinic-db'
  user: '$POSTGRES_USER:postgres'
  password: '$POSTGRES_PASSWORD:postgres'

api:
  backend-server:
    url: '$BACKEND_SERVER_URL:http://localhost:9000'